# DB/Prisma Setup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **DO NOT COMMIT.** Per user preference, no `git commit` is run by implementers or controllers. All changes stay in the working tree; the user decides when and how to commit.

**Goal:** mock `src/lib/posts.ts`를 Supabase Postgres + Prisma 기반 실제 DB 쿼리로 교체하고, 읽기 경로(`/`, `/posts`, `/posts/[slug]`)가 DB의 글을 가져와 렌더링하게 만든다.

**Architecture:** Prisma schema에 `Post`/`Tag` 모델을 정의하고 Supabase 단일 프로젝트에 마이그레이션. `src/lib/db.ts` 싱글톤으로 PrismaClient 캐싱(Next.js dev HMR 대응). `src/lib/posts.ts`의 공개 함수 시그니처(`getPosts`, `getPost`)는 보존해 호출부 무수정. 시드는 `prisma/seed.ts`로 idempotent upsert. Vercel 빌드 시 `migrate deploy` 자동 적용.

**Tech Stack:** Prisma 6+, `@prisma/client`, `tsx` (seed runner), Supabase Postgres (Pooler URL + Direct URL), Next.js 16, TypeScript strict.

**Reference spec:** `docs/superpowers/specs/2026-05-22-db-prisma-design.md`

---

## Pre-flight

- 작업 디렉토리: `/Users/jiseong-in/Documents/GitHub/blog`
- 현재 상태: 베이스 스캐폴딩이 **uncommitted**로 working tree에 있음 (`git status`에서 untracked로 표시). Initial commit `be815be`이 HEAD.
- 작업 결과 또한 uncommitted로 둔다. 어떤 태스크도 `git add` / `git commit` 실행 금지.

---

### Task 1: Prisma 의존성 설치

**Files:**
- Modify: `package.json` (dependencies/devDependencies — pnpm이 자동 갱신)
- Modify: `pnpm-lock.yaml` (자동)

- [ ] **Step 1: dev 의존성 설치**

Run:
```bash
pnpm add -D prisma tsx
```

Expected: `package.json`의 `devDependencies`에 `prisma`, `tsx` 추가. 출력에 설치 완료 표시.

- [ ] **Step 2: 런타임 의존성 설치**

Run:
```bash
pnpm add @prisma/client
```

Expected: `package.json`의 `dependencies`에 `@prisma/client` 추가.

- [ ] **Step 3: 설치 검증**

Run:
```bash
pnpm prisma -v
```

Expected: Prisma CLI 버전 출력 (6.x 이상). 에러 없음.

(커밋하지 않음.)

---

### Task 2: Prisma 초기화 + schema 작성

**Files:**
- Create: `prisma/schema.prisma`

`pnpm prisma init`은 `.env`를 새로 만들고 schema를 stub으로 생성한다. 이 프로젝트는 이미 `.env.example`이 있고 `.env.local`이 표준이므로 `init`은 쓰지 않고 직접 schema만 작성한다.

- [ ] **Step 1: `prisma/` 디렉토리 생성**

Run:
```bash
mkdir -p prisma
```

- [ ] **Step 2: `prisma/schema.prisma` 작성**

Create the file with exactly:

```prisma
datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Post {
  id          String   @id @default(cuid())
  slug        String   @unique
  title       String
  description String
  contentHtml String
  published   Boolean  @default(true)
  publishedAt DateTime
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  tags        Tag[]    @relation("PostTags")

  @@index([published, publishedAt(sort: Desc)])
}

model Tag {
  id    String @id @default(cuid())
  name  String
  slug  String @unique
  posts Post[] @relation("PostTags")
}
```

- [ ] **Step 3: schema 유효성 검사**

Run:
```bash
pnpm prisma validate
```

Expected: `The schema is valid` 메시지. 에러 없음.

만약 환경변수 미설정 에러가 뜨면 (`DATABASE_URL` not found 등), 일단 dummy 값으로 검사할 수 있도록 다음과 같이 실행:

```bash
DATABASE_URL="postgresql://x" DIRECT_URL="postgresql://x" pnpm prisma validate
```

(커밋하지 않음.)

---

### Task 3: ★ USER ACTION REQUIRED — Supabase 자격증명 입력

**Files:**
- Modify: `.env.local` (사용자가 직접 작성/수정)

이 태스크는 **사용자가 직접 수행한다**. 구현자(implementer 서브에이전트)는 이 태스크를 만나면 다음 안내 메시지를 출력하고 BLOCKED 상태로 보고한다. 컨트롤러는 사용자에게 안내한 뒤 사용자가 완료했음을 확인하면 다음 태스크를 진행한다.

- [ ] **Step 1: 사용자가 Supabase 프로젝트를 생성 (없으면)**

[supabase.com](https://supabase.com)에서 새 프로젝트 생성. 또는 기존 사용 프로젝트 선택.

- [ ] **Step 2: 사용자가 Connection Strings 복사**

Supabase 프로젝트 대시보드 → Project Settings → Database → Connection string 섹션에서:

- **Transaction (port 6543, pgbouncer)** 의 URI를 복사 → `DATABASE_URL` 값으로
- **Session (port 5432, direct)** 의 URI를 복사 → `DIRECT_URL` 값으로

비밀번호는 프로젝트 생성 시 설정한 DB 비밀번호.

- [ ] **Step 3: 사용자가 `.env.local`에 두 키 작성**

`/Users/jiseong-in/Documents/GitHub/blog/.env.local` 파일에 다음 형식으로:

```
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1
DIRECT_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres
```

`<project-ref>`, `<password>`, `<region>`은 실제 값으로 치환.

`.env.local`은 `.gitignore`에 포함되어 커밋되지 않음 (이미 패턴 등록됨).

- [ ] **Step 4: 컨트롤러가 연결 검증**

사용자가 위 3단계를 완료했음을 알리면, 컨트롤러는 다음 명령으로 연결을 검증:

```bash
pnpm prisma db pull --print | head -5
```

Expected: 빈 DB라 모델 정의가 출력되지 않지만, "Connection successful" 또는 "Introspection complete" 류의 메시지. 에러가 뜨면 `.env.local`의 URL 형식 또는 비밀번호 확인.

또는 더 단순하게:

```bash
pnpm prisma migrate status
```

Expected: "Database connection successful." 또는 "No migration found" — 둘 다 연결 자체는 정상이라는 신호.

(커밋하지 않음.)

---

### Task 4: 첫 마이그레이션 적용

**Files:**
- Create: `prisma/migrations/YYYYMMDDHHMMSS_init/migration.sql` (Prisma가 자동 생성)
- Create: `prisma/migrations/migration_lock.toml` (Prisma가 자동 생성)

- [ ] **Step 1: 마이그레이션 생성 + 적용**

Run:
```bash
pnpm prisma migrate dev --name init
```

Expected:
- `prisma/migrations/<timestamp>_init/migration.sql` 파일 생성
- DB에 `Post`, `Tag`, `_PostTags` 테이블 + 인덱스 적용
- `Your database is now in sync with your schema.` 메시지

만약 "shadow database" 관련 에러가 뜨면 (`P3014` 등), Supabase는 shadow DB 권한이 없을 수 있다. 이 경우:

```bash
pnpm prisma db push
```

로 폴백한다. `db push`는 마이그레이션 파일을 만들지 않고 schema를 직접 적용. (장기적으로는 별도 dev DB 분리 시 `migrate dev`로 복귀.)

- [ ] **Step 2: 생성 결과 확인**

Run:
```bash
pnpm prisma migrate status
```

Expected: "Database schema is up to date!" 또는 동등한 메시지.

(`db push` 폴백을 썼다면 이 명령은 "No migration found in prisma/migrations"라고 할 수 있음 — `prisma db pull --print | head -30`로 테이블 존재만 확인.)

- [ ] **Step 3: 생성된 클라이언트 확인**

Run:
```bash
ls node_modules/.prisma/client/
```

Expected: `index.js`, `index.d.ts` 등 파일 존재. (`prisma generate`가 `migrate dev` 도중 자동 실행됨.)

(커밋하지 않음.)

---

### Task 5: Seed 스크립트 작성

**Files:**
- Create: `prisma/seed.ts`
- Modify: `package.json` (`prisma.seed` 설정 추가)

- [ ] **Step 1: `prisma/seed.ts` 작성**

Create the file with exactly:

```ts
import { PrismaClient } from "@prisma/client";

const db = new PrismaClient();

const TAGS = [
  { slug: "meta", name: "meta" },
  { slug: "typescript", name: "typescript" },
  { slug: "tooling", name: "tooling" },
  { slug: "next", name: "next" },
  { slug: "architecture", name: "architecture" },
];

const POSTS = [
  {
    slug: "hello-world",
    title: "Hello, world",
    description: "첫 번째 글 — 블로그를 만든 이유에 관해.",
    contentHtml:
      "<p>이 블로그는 내가 배우고 만든 것들을 기록하는 공간이다.</p>",
    publishedAt: new Date("2026-05-01T00:00:00.000Z"),
    tagSlugs: ["meta"],
  },
  {
    slug: "typescript-strict-mode",
    title: "TypeScript strict mode를 켜야 하는 이유",
    description: "타입 안정성과 리팩토링 안전성 사이의 관계.",
    contentHtml:
      "<p>strict 모드는 단순한 옵션이 아니라 개발 문화의 선택이다.</p>",
    publishedAt: new Date("2026-05-10T00:00:00.000Z"),
    tagSlugs: ["typescript", "tooling"],
  },
  {
    slug: "next-app-router-server-components",
    title: "App Router에서 서버 컴포넌트를 기본값으로",
    description: "클라이언트 컴포넌트는 잎(leaf)에만 둔다.",
    contentHtml:
      "<p>서버에서 데이터에 직접 접근할 수 있다면, 그렇게 한다.</p>",
    publishedAt: new Date("2026-05-18T00:00:00.000Z"),
    tagSlugs: ["next", "architecture"],
  },
];

async function main() {
  for (const t of TAGS) {
    await db.tag.upsert({
      where: { slug: t.slug },
      update: { name: t.name },
      create: t,
    });
  }
  for (const p of POSTS) {
    const { tagSlugs, ...rest } = p;
    await db.post.upsert({
      where: { slug: p.slug },
      update: {
        ...rest,
        tags: { set: tagSlugs.map((slug) => ({ slug })) },
      },
      create: {
        ...rest,
        tags: { connect: tagSlugs.map((slug) => ({ slug })) },
      },
    });
  }
}

main()
  .then(() => db.$disconnect())
  .catch(async (e) => {
    console.error(e);
    await db.$disconnect();
    process.exit(1);
  });
```

- [ ] **Step 2: `package.json`에 `prisma.seed` 설정 추가**

Read `package.json` 먼저. 최상위 객체에 다음 키를 추가 (기존 키와 동급, `scripts`와 같은 레벨):

```json
"prisma": {
  "seed": "tsx prisma/seed.ts"
}
```

다른 키 변경 없음. 들여쓰기는 기존 스타일에 맞춤.

- [ ] **Step 3: 시드 실행**

Run:
```bash
pnpm prisma db seed
```

Expected: 에러 없이 종료. 출력 메시지에 "Running seed command..." → "The seed command has been executed."

- [ ] **Step 4: 시드 결과 확인**

Run:
```bash
pnpm prisma studio
```

브라우저가 자동으로 열리거나 `http://localhost:5555` 안내. `Tag` 테이블 5개, `Post` 테이블 3개, 각 Post의 tags 관계 확인 후 종료(Ctrl+C).

또는 CLI로 확인:

```bash
node -e "const { PrismaClient } = require('@prisma/client'); const db = new PrismaClient(); db.post.count().then(n => console.log('posts:', n)).then(() => db.tag.count()).then(n => console.log('tags:', n)).then(() => db.\$disconnect())"
```

Expected: `posts: 3`, `tags: 5`.

- [ ] **Step 5: idempotent 검증 — 다시 시드 실행해도 안전**

Run:
```bash
pnpm prisma db seed
```

Expected: 다시 에러 없이 종료. count는 여전히 3 / 5 (중복 생성 없음, upsert가 동작).

(커밋하지 않음.)

---

### Task 6: Prisma Client 싱글톤 (`src/lib/db.ts`)

**Files:**
- Create: `src/lib/db.ts`

- [ ] **Step 1: `src/lib/db.ts` 작성**

Create the file with exactly:

```ts
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma?: PrismaClient;
};

export const db = globalForPrisma.prisma ?? new PrismaClient();

if (process.env.NODE_ENV !== "production") {
  globalForPrisma.prisma = db;
}
```

- [ ] **Step 2: typecheck**

Run:
```bash
pnpm typecheck
```

Expected: 에러 없음. (`@prisma/client`의 `PrismaClient` 타입이 잘 해결됨.)

(커밋하지 않음.)

---

### Task 7: `src/lib/posts.ts` Prisma 기반으로 재작성

**Files:**
- Modify: `src/lib/posts.ts` (전체 교체)

- [ ] **Step 1: 현재 `src/lib/posts.ts` 읽기**

호출부에 영향이 없도록 export 시그니처를 확인한다. `Post` 타입과 `getPosts`/`getPost` 함수가 존재해야 한다.

- [ ] **Step 2: 전체 교체**

Overwrite `src/lib/posts.ts` with exactly:

```ts
import { db } from "./db";

export type Post = {
  slug: string;
  title: string;
  description: string;
  contentHtml: string;
  publishedAt: string;
  tags: string[];
};

type PostRow = {
  slug: string;
  title: string;
  description: string;
  contentHtml: string;
  publishedAt: Date;
  tags: { name: string }[];
};

function toPost(row: PostRow): Post {
  return {
    slug: row.slug,
    title: row.title,
    description: row.description,
    contentHtml: row.contentHtml,
    publishedAt: row.publishedAt.toISOString(),
    tags: row.tags.map((t) => t.name),
  };
}

export async function getPosts(): Promise<Post[]> {
  const rows = await db.post.findMany({
    where: { published: true },
    orderBy: { publishedAt: "desc" },
    select: {
      slug: true,
      title: true,
      description: true,
      contentHtml: true,
      publishedAt: true,
      tags: { select: { name: true } },
    },
  });
  return rows.map(toPost);
}

export async function getPost(slug: string): Promise<Post | null> {
  const row = await db.post.findUnique({
    where: { slug },
    select: {
      slug: true,
      title: true,
      description: true,
      contentHtml: true,
      published: true,
      publishedAt: true,
      tags: { select: { name: true } },
    },
  });
  if (!row || !row.published) return null;
  return toPost(row);
}
```

- [ ] **Step 3: typecheck**

Run:
```bash
pnpm typecheck
```

Expected: 에러 없음.

- [ ] **Step 4: dev 서버에서 페이지 렌더 확인**

Run:
```bash
pnpm dev &
sleep 5
echo "home:"     ; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/
echo "posts:"    ; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/posts
echo "existing:" ; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/posts/hello-world
echo "404 check:"; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/posts/does-not-exist
kill %1 2>/dev/null || true
wait 2>/dev/null || true
```

Expected:
```
home: 200
posts: 200
existing: 200
404 check: 404
```

추가로 `curl -s http://localhost:3000/posts | grep -c "hello-world"`가 1 이상이면 시드 데이터가 실제로 렌더되고 있음을 확인.

(커밋하지 않음.)

---

### Task 8: `package.json` 빌드/postinstall 스크립트 정비

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 현재 `package.json` 읽기**

`scripts` 객체를 확인한다.

- [ ] **Step 2: `postinstall` 추가, `build` 수정**

`scripts` 객체를 다음 상태로 만든다 (다른 키는 보존):

```json
"scripts": {
  "dev": "next dev",
  "build": "prisma migrate deploy && next build",
  "start": "next start",
  "lint": "eslint",
  "typecheck": "tsc --noEmit",
  "postinstall": "prisma generate"
}
```

변경:
- `build`: `next build` → `prisma migrate deploy && next build`
- 추가: `postinstall`: `prisma generate`
- 그 외 키(`dev`, `start`, `lint`, `typecheck`): 변경 없음

- [ ] **Step 3: 로컬에서 빌드 검증**

Run:
```bash
pnpm build
```

Expected: `prisma migrate deploy`가 "No pending migrations" (또는 첫 회 적용 메시지) 후 `next build`가 성공적으로 완료.

만약 Task 4에서 `db push` 폴백을 썼다면 마이그레이션 파일이 없어 `migrate deploy`가 무 동작이지만 에러는 아님. 정상.

- [ ] **Step 4: postinstall 동작 검증**

Run:
```bash
pnpm install
```

Expected: 설치 끝에 `prisma generate`가 자동 실행. "✔ Generated Prisma Client" 메시지.

(커밋하지 않음.)

---

### Task 9: 최종 인수 기준 검증

**Files:** (변경 없음 — 검증만)

스펙 §12의 9개 인수 기준을 하나씩 확인한다. 실패 시 그 자리에서 수정 (단, 절대 커밋하지 않음).

- [ ] **Step 1: §12.1 schema 유효성**

Run:
```bash
pnpm prisma validate
```

Expected: "The schema is valid."

- [ ] **Step 2: §12.2 마이그레이션 또는 db push 결과**

Run:
```bash
pnpm prisma migrate status || pnpm prisma db pull --print | head -10
```

Expected: 마이그레이션이 적용되었거나(`db push`로 했을 경우) DB에서 schema를 introspect 가능. 둘 중 하나 OK.

- [ ] **Step 3: §12.3 시드 데이터 존재 + idempotent**

Run:
```bash
pnpm prisma db seed
node -e "const { PrismaClient } = require('@prisma/client'); const db = new PrismaClient(); Promise.all([db.post.count(), db.tag.count()]).then(([p, t]) => { console.log('posts:', p, 'tags:', t); db.\$disconnect(); })"
```

Expected: `posts: 3 tags: 5`.

- [ ] **Step 4: §12.4 + §12.5 페이지 렌더 (200/404)**

Run:
```bash
pnpm dev &
sleep 5
for path in / /posts /posts/hello-world; do
  code=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:3000${path}")
  echo "${path}: ${code}"
done
echo "non-existent:"
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/posts/does-not-exist
echo "render check:"
curl -s http://localhost:3000/posts | grep -c "hello-world"
curl -s http://localhost:3000/posts/hello-world | grep -c "이 블로그는"
kill %1 2>/dev/null || true
wait 2>/dev/null || true
```

Expected:
```
/: 200
/posts: 200
/posts/hello-world: 200
non-existent:
404
render check:
1
1
```

- [ ] **Step 5: §12.6 빌드 통과**

Run:
```bash
pnpm build
```

Expected: 빌드 성공. (이미 Task 8에서 검증했으나 최종 한 번 더 확인.)

- [ ] **Step 6: §12.7 typecheck / lint 통과**

Run:
```bash
pnpm typecheck && pnpm lint
```

Expected: 둘 다 에러 없이 종료.

- [ ] **Step 7: §12.8 lib/posts.ts 시그니처 보존**

Read `src/lib/posts.ts`. Confirm:
- `export type Post = { slug, title, description, contentHtml, publishedAt, tags }`
- `export async function getPosts(): Promise<Post[]>`
- `export async function getPost(slug: string): Promise<Post | null>`

이 시그니처가 그대로면 호출부(`(public)/page.tsx`, `(public)/posts/page.tsx`, `(public)/posts/[slug]/page.tsx`)는 무수정 상태로 동작.

Run (호출부 변경 없음 확인):
```bash
git diff --name-only -- 'src/app/(public)/'
```

Expected: 출력 없음 (HEAD가 Initial commit이라 src/app/(public)/* 는 모두 untracked이므로 `git diff`는 무 표시 — Initial commit과 비교).

좀 더 명확한 검증:

```bash
grep -h "from \"@/lib/posts\"" src/app/\(public\)/page.tsx src/app/\(public\)/posts/page.tsx src/app/\(public\)/posts/\[slug\]/page.tsx
```

Expected: 3개 라인이 출력되며 모두 `import` 문이 정상.

- [ ] **Step 8: §12.9 Prisma Studio 동작**

Run:
```bash
pnpm prisma studio
```

브라우저에서 `Post`, `Tag` 테이블의 시드 데이터를 확인한 뒤 Ctrl+C로 종료.

또는 시간을 아끼려면 이 검증은 스킵하고 Step 3의 count 결과로 갈음.

- [ ] **Step 9: 최종 정리**

이 시점에서 working tree에는:
- 새 파일: `prisma/schema.prisma`, `prisma/seed.ts`, `prisma/migrations/...`, `src/lib/db.ts`
- 수정: `src/lib/posts.ts`, `package.json`, `pnpm-lock.yaml`

사용자에게 보고하고 커밋 여부는 사용자에게 맡긴다.

(커밋하지 않음.)

---

## Self-Review Notes

**Spec coverage:**
- §2 In scope: Supabase 연결 (T3), Prisma schema (T2), 첫 마이그레이션 (T4), seed (T5), 클라이언트 싱글톤 (T6), posts.ts 재작성 (T7), 환경변수 활용 (T3 — `.env.example` 자체는 베이스 스캐폴딩이 이미 만들어 둠), package.json 정비 (T8) — 모두 매핑됨
- §3 Risk: T3 안내문과 §13에 명시
- §12 Acceptance criteria 9개: T9의 9개 step에 1:1 매핑

**Placeholder scan:**
- T3에 `<project-ref>`, `<password>`, `<region>` 있으나 이는 사용자가 채울 자리이고 의도된 placeholder (사용자 액션이라 명시됨)
- "TBD", "implement later" 등 plan-failure 패턴은 없음

**Type consistency:**
- `Post` 타입은 T7에서 한 번 정의됨 — `slug`, `title`, `description`, `contentHtml`, `publishedAt: string`, `tags: string[]`
- `PostRow` 내부 타입은 Prisma select 결과 모양과 일치
- `getPosts`/`getPost` 시그니처는 베이스 스캐폴딩의 mock 시그니처와 동일 (string slug → Promise<Post[]|null>)

**Commit hygiene:**
- 모든 태스크 끝에 "(커밋하지 않음.)" 명시
- 어떤 step도 `git commit` 명령을 포함하지 않음
- T9 Step 9에서 사용자에게 결정 위임

---

## Execution Handoff

플랜 저장 후 사용자에게 실행 방식을 묻는다:

1. Subagent-Driven (권장) — 태스크당 신선한 subagent 디스패치, 사이에 리뷰. T3 (USER ACTION)에서 자연스럽게 일시정지.
2. Inline Execution — 현재 세션에서 일괄 실행, T3에서 사용자 입력 받기 위해 정지.

어떤 방식이든 **커밋은 절대 자동으로 하지 않음**.
